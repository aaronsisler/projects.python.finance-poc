# projects.python.finance-poc

## Notes from Python Docs

```bash
conda env export --prefix ./venv --from-history > environment.yml
```

```bash
conda env create --prefix ./venv -f environment.yml
```
